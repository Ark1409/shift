module shift;
use shift;
use shift;
use sshift.     lang    ;
use shift.io;

class my_name {
    use shift;
    use shift.lang.io;
}

class my_name {
    use test;
}

class file {

}

class my_file {
    private static file[][] own_file = 3;
    private static int own_file = 44;
    private static long own_file = 'f'+'d';

    private void runnner(int a,long,double a,short a) {
        
    }
}
